@page
@using BibliotekaOnline.Areas.Identity.Pages.Account.Manage
@model ChangePasswordModel
@{
    ViewData["Title"] = "Zmień hasło";
    ViewData["ActivePage"] = "ChangePassword";
    Layout = "_ManageLayout";
}

<partial name="_StatusMessage" for="StatusMessage" />

<div class="modern-card">
            
            <form id="change-password-form" method="post">
                <div asp-validation-summary="ModelOnly" class="modern-alert modern-alert-danger" role="alert"></div>
                
                <div class="modern-form-group">
                    <label asp-for="Input.OldPassword" class="modern-label">
                        🔒 Obecne hasło
                    </label>
                    <input asp-for="Input.OldPassword" class="modern-input" autocomplete="current-password" aria-required="true" placeholder="Wprowadź obecne hasło" type="password" />
                    <span asp-validation-for="Input.OldPassword" class="text-danger"></span>
                </div>
                
                <div class="modern-form-group">
                    <label asp-for="Input.NewPassword" class="modern-label">
                        🔑 Nowe hasło
                    </label>
                    <input asp-for="Input.NewPassword" class="modern-input" autocomplete="new-password" aria-required="true" placeholder="Wprowadź nowe hasło" type="password" />
                    <span asp-validation-for="Input.NewPassword" class="text-danger"></span>
                </div>
                
                <div class="modern-form-group">
                    <label asp-for="Input.ConfirmPassword" class="modern-label">
                        ✅ Potwierdź nowe hasło
                    </label>
                    <input asp-for="Input.ConfirmPassword" class="modern-input" autocomplete="new-password" aria-required="true" placeholder="Powtórz nowe hasło" type="password" />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                </div>
                
                <button type="submit" class="modern-btn w-100">
                    🚀 Zmień hasło
                </button>
            </form>
            
            <div class="info-box mt-3">
                <h6>💡 Wskazówki bezpieczeństwa</h6>
                <div class="d-flex align-items-center mb-2">
                    Używaj co najmniej 6 znaków
                </div>
                <div class="d-flex align-items-center mb-2">
                    Kombinuj małe i wielkie litery
                </div>
                <div class="d-flex align-items-center mb-2">
                    Dodaj cyfry dla większego bezpieczeństwa
                </div>
                <div class="d-flex align-items-center">
                    Unikaj używania osobistych informacji
                </div>
            </div>
        </div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} 